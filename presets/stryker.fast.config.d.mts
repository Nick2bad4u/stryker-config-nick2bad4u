import type { SharedStrykerOptions } from "../dist/stryker-config.js";

/** Directly runnable fast local-iteration Stryker configuration. */
declare const strykerFastConfig: SharedStrykerOptions;

export default strykerFastConfig;
